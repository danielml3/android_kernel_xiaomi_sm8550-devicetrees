DTC_INCLUDE += $(srctree)/../socrates-modules/qcom/opensource/camera-kernel

include $(srctree)/$(src)/config/kalama.mk

always-y	:= $(dtbo-y) $(dtb-y)
subdir-y	:= $(dts-dirs)
clean-files	:= *.dtb *.dtbo
